spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pools_test
    username: pools_user
    password: pass
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true