spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pools_test
    username: pools_user
    password: pass
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true